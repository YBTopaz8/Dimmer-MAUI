<metadata>
	<!-- 
       REMOVE the "kotlin" rename attribute. 
       We want the Kotlin types to come from the NuGet package, 
       not be generated by this binding project.
  -->

	<!-- 
       Fix for the "Companion" warnings (BG8401).
       Kotlin creates static classes named "Companion". C# doesn't like 
       types having the same name as inner members sometimes. 
       We rename them to avoid conflicts.
  -->
	<remove-node path="/api/package[@name='me.ibrahimsn.lib']/class[@name='AccessibleExploreByTouchHelper']" />
	<attr path="/api/package[@name='me.ibrahimsn.lib']/class[@name='NavigationComponentHelper']/field[@name='Companion']" name="managedName">CompanionInstance</attr>
	<attr path="/api/package[@name='me.ibrahimsn.lib']/class[@name='SmoothBottomBar']/field[@name='Companion']" name="managedName">CompanionInstance</attr>
</metadata>