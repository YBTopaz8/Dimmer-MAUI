<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Dimmer.WinUI.Views.DimmerLiveUI.DimmerLivePage"
 xmlns:windows="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.WindowsSpecific;assembly=Microsoft.Maui.Controls"
 xmlns:vm="clr-namespace:Dimmer.WinUI.ViewModel"
 xmlns:modeltwo="clr-namespace:Dimmer.WinUI.Utils.Models"
             
              xmlns:converters="clr-namespace:Dimmer.Utilities.TypeConverters;assembly=Dimmer"
 xmlns:cw="clr-namespace:Dimmer.WinUI.Views.CustomViews"
 xmlns:lang="clr-namespace:Dimmer.Resources.Localization;assembly=Dimmer"
 xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
 xmlns:model="clr-namespace:Dimmer.Data.ModelView;assembly=Dimmer"
             xmlns:models="clr-namespace:Dimmer.DimmerLive.Models;assembly=Dimmer"
 xmlns:dimmSearch="clr-namespace:Dimmer.Data.ModelView.DimmerSearch;assembly=Dimmer"
 xmlns:modelsDb="clr-namespace:Dimmer.Data.Models;assembly=Dimmer"
 xmlns:syncf="http://schemas.syncfusion.com/maui/toolkit"
 xmlns:tql="clr-namespace:Dimmer.DimmerSearch.TQL;assembly=Dimmer"
 xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
 xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
 x:Name="myPage"
             x:DataType="vm:BaseViewModelWin"
             Title="DimmerLivePage">

    <ContentPage.Resources>
        <converters:DurationConverterFromMsToTimeSpan x:Key="DurationConverter" />
        <converters:BytesToMegabytesConverter x:Key="FileSizeConverter" />
        <converters:BytesArrayToImageSource x:Key="BytesToImageConverter" />
        <converters:BoolToInverseConverter x:Key="BoolToInverse" />
        <converters:IndexToVisibilityConverter x:Key="IndexToVisibilityConverter" />
        <converters:DateTimeToLocalDateConverter x:Key="DateTimeConverter" />
        <converters:DateTimeToLocalTimeConverter x:Key="DateTimeToTimeConverter" />
        <converters:BoolToYesNoConverter x:Key="BoolToYesNo" />
        <converters:VolumeConverter x:Key="VolConverter" />
        <ResourceDictionary />
    </ContentPage.Resources>


    <Grid RowDefinitions="*"
          x:Name="MainGrid">

        <syncf:SfTabView SelectedIndex="{Binding DimmerLiveViewModel.SelectedIndex}">
            <syncf:SfTabItem Header="OG Login">
                <syncf:SfTabItem.Content>

                    <VerticalStackLayout>

                        <Border x:Name="LoginSignUp" StyleClass="Elevation2">

                            <syncf:SfTabView
                 x:Name="LoginSignUpTab">
                                <syncf:SfTabItem>
                                    <syncf:SfTabItem.Content>
                                        <VerticalStackLayout HorizontalOptions="Center" Spacing="10">
                                            <Label 
                                 Text="Login"
                                 FontSize="24"
                                 HorizontalOptions="Center" />
                                            <Label Text="{Binding LoginViewModel.CurrentUser.Username}"/>
                                            <Label Text="{Binding LoginViewModel.CurrentUser.Email}"/>
                                            <Label
                                 Text="Please login to continue"
                                 FontSize="16"
                                 HorizontalOptions="Center" />


                                            <Entry
                                 Text="{Binding LoginViewModel.Username}"
                                 x:Name="UsernameEntry"
                                 Placeholder="Enter your username"
                                 FontSize="16"
                                 HorizontalOptions="Fill" />
                                            <Entry
                                 Text="{Binding LoginViewModel.Email}"
                                 x:Name="EmailEntry"
                                 Placeholder="Enter your Email"
                                 FontSize="16"
                                 HorizontalOptions="Fill" />


                                            <Entry
                                 x:Name="PasswordEntry"
                                 Placeholder="Enter your password"
                                 IsPassword="True"
                                 Text="{Binding LoginViewModel.Password}"
                                 FontSize="16"
                                 HorizontalOptions="Fill" />

                                            <HorizontalStackLayout
                                 Spacing="10"
                                 HorizontalOptions="Center">

                                                <Button
                                 Text="Login"
                                 FontSize="16"
                                 Command="{Binding LoginViewModel.LoginCommand}"
                                 HorizontalOptions="Center"
                                />


                                                <Button
                                 Command="{Binding LoginViewModel.RegisterCommand}"
                                 Text="Sign Up"
                                 FontSize="16"
                                 HorizontalOptions="Center"
                                 />

                                            </HorizontalStackLayout>



                                        </VerticalStackLayout>
                                    </syncf:SfTabItem.Content>
                                </syncf:SfTabItem>
                            </syncf:SfTabView>

                        </Border>




                    </VerticalStackLayout>


                </syncf:SfTabItem.Content>
            </syncf:SfTabItem>
            <syncf:SfTabItem Header="Login">
                <syncf:SfTabItem.Content>
                    <VerticalStackLayout>
                        <ScrollView x:Name="TabA">
                            <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

                                <Label Text="Dimmer Live" FontSize="Header" HorizontalOptions="Center" />
                                <Label Text="Sign in or create an account" HorizontalOptions="Center" />
                                <Label Text="{Binding LoginViewModel.IsAuthenticated}"/>
                                <Label Text="{Binding LoginViewModel.Username}"/>


                                <Button Text="Logout"
                                    Command="{Binding LoginViewModel.LogoutCommand}"
                                       />
                                <Label Text="{Binding LoginViewModel.CurrentUser.ProfileImageFile.Name}"
                                       />

                                <Image
                                    HeightRequest="250"
                                    WidthRequest="250"
                                    Source="{Binding LoginViewModel.CurrentUser.ProfileImageFile.Url}"
                                    
                                    />

                                <Button Text="UpdateProfileImage"
                                        Command="{Binding LoginViewModel.PickImageFromDeviceCommand}"/>
                                <Label Text="ddd"
                                       />


                                <FlexLayout JustifyContent="SpaceBetween">
                                    <VerticalStackLayout>
                                        <Label Text="Username"/>
                                        <Label Text="Email"/>
                                        <Label Text="Date Joined"/>
                                    </VerticalStackLayout>

                                    <VerticalStackLayout>
                                        <Label Text="{Binding LoginViewModel.CurrentUser.Username}"/>
                                        <Label Text="{Binding LoginViewModel.CurrentUser.Email}"/>
                                        <Label Text="{Binding LoginViewModel.CurrentUser.CreatedAt}"/>

                                    </VerticalStackLayout>
                                </FlexLayout>

                                <!-- Error Message Display -->
                                <Label Text="{Binding LoginViewModel.ErrorMessage}"
                   TextColor="Red"
                   IsVisible="{Binding LoginViewModel.ErrorMessage, Converter={converters:CountToVisibilityConverter}}"
                   HorizontalOptions="Center" />
                                <VerticalStackLayout IsVisible="{Binding LoginViewModel.IsAuthenticated , Converter={converters:BoolToInverseConverter}}">

                                    <!-- Input Fields -->
                                    <Entry Placeholder="Username" Text="{Binding LoginViewModel.CurrentUser.Username}" IsEnabled="{Binding IsBusy, Converter={converters:BoolToInverseConverter}}" />
                                    <Entry Placeholder="Email (for registration/reset)" Text="{Binding LoginViewModel.Email}" IsEnabled="{Binding IsBusy, Converter={converters:BoolToInverseConverter}}" />
                                    <Entry Placeholder="Password" Text="{Binding LoginViewModel.Password}" IsPassword="True" IsEnabled="{Binding IsBusy, Converter={converters:BoolToInverseConverter}}" />

                                    <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

                                    <!-- Action Buttons -->
                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                                        <Button Grid.Column="0" Text="Login" Command="{Binding LoginViewModel.LoginCommand}" IsEnabled="{Binding IsBusy, Converter={converters:BoolToInverseConverter}}" />
                                        <Button Grid.Column="1" Text="Register" Command="{Binding LoginViewModel.RegisterCommand}" IsEnabled="{Binding IsBusy, Converter={converters:BoolToInverseConverter}}" />
                                    </Grid>

                                    <Button Text="Forgot Password?" Command="{Binding LoginViewModel.ForgotPasswordCommand}" IsEnabled="{Binding IsBusy, Converter={converters:BoolToInverseConverter}}" HorizontalOptions="Center" />

                                </VerticalStackLayout>
                            </VerticalStackLayout>
                        </ScrollView>
                    </VerticalStackLayout>
                </syncf:SfTabItem.Content>
            </syncf:SfTabItem>

            <syncf:SfTabItem Header="Conversations">
                <syncf:SfTabItem.Content>
                    <Grid RowDefinitions="*,auto">
                        <!-- List of Conversations -->
                        <CollectionView Grid.Row="0"  BackgroundColor="Transparent"
                            ItemsSource="{Binding  DimmerLiveViewModel.MessagesInGroupConversation}"
                            SelectionMode="Single" EmptyView="No Convos"
                            >
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:ChatMessage">
                                    <Border Padding="10" Margin="5" StrokeShape="RoundRectangle 8">
                                        <VerticalStackLayout>

                                            <HorizontalStackLayout Spacing="5">
                                                <HorizontalStackLayout.Resources>
                                                    <Style TargetType="syncf:SfChip">
                                                        <Setter Property="FontSize" Value="11"/>
                                                        <Setter Property="TextColor" Value="Grey"/>
                                                    </Style>
                                                </HorizontalStackLayout.Resources>
                                                <syncf:SfChip Text="{Binding UserDeviceIdiom}" />

                                                <syncf:SfChip Text="{Binding SharedSong.Title}"
                                                              IsVisible="{Binding SharedSong, Converter={converters:IsNullTrueOrFalseConverter}}"
                                                              />

                                            </HorizontalStackLayout>
                                            <Label Text="{Binding UserName}" FontSize="11" />
                                            <Label Text="{Binding Text}" FontSize="41"/>





                                            <HorizontalStackLayout Spacing="5">
                                                <HorizontalStackLayout.Resources>
                                                    <Style TargetType="syncf:SfChip">
                                                        <Setter Property="FontSize" Value="11"/>
                                                        <Setter Property="TextColor" Value="Grey"/>
                                                    </Style>
                                                </HorizontalStackLayout.Resources>
                                                <syncf:SfChip Text="{Binding UserDeviceIdiom}" />
                                                <syncf:SfChip Text="{Binding CreatedAt}" />
                                                

                                            </HorizontalStackLayout>
                                            <VerticalStackLayout>
                                                
                                            </VerticalStackLayout>
                                        </VerticalStackLayout>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <!-- Message Display and Input for Selected Chat -->
                        <Grid Grid.Row="1" Padding="10" Margin="5"
                              ColumnDefinitions="*,auto"
                              BackgroundColor="Transparent">
                            <Border Grid.Column="0">
                                    <Editor x:Name="NewMessage"
                                            Text="{Binding DimmerLiveViewModel.NewMessageText}"
                                            />
                            </Border>

                            <VerticalStackLayout Grid.Column="1">
                                <Button Command="{Binding DimmerLiveViewModel.SendMessageCommand}"
                                          />
                            </VerticalStackLayout>
                        </Grid>
                    </Grid>
                </syncf:SfTabItem.Content>
            </syncf:SfTabItem>

            <syncf:SfTabItem Header="Friends">
                <syncf:SfTabItem.Content>
                    <Grid RowDefinitions="Auto, *">

                        <!-- Refresh Button -->
                        <Button Grid.Row="0" Text="Refresh User List" Command="{Binding DimmerLiveViewModel.LoadAllUsersCommand}" />

                        <!-- List of All Users -->
                        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding DimmerLiveViewModel.AllUsers}"
                        SelectionMode="Single">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:UserModelOnline">
                                    <Border Padding="10" Margin="5">
                                        <Border.GestureRecognizers>
                                            <!-- When a user taps on a row, execute the command -->
                                            <TapGestureRecognizer 
                                                Tapped="ConvoStart_Tapped"
                                                x:Name="ConvoStart"
                                                  CommandParameter="{Binding .}" />
                                        </Border.GestureRecognizers>
                                        <Label Text="{Binding Username}" />
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                    </Grid>
                </syncf:SfTabItem.Content>
            </syncf:SfTabItem>

            <syncf:SfTabItem Header="Requests">
                <syncf:SfTabItem.Content>
                    <VerticalStackLayout Spacing="10">
                        <!-- Section to Send a New Request -->
                        <Border Padding="10">
                            <VerticalStackLayout>
                                <Label Text="Send a New Friend Request" FontAttributes="Bold"/>
                                <Entry x:Name="FriendUsernameEntry" Placeholder="Enter username" />
                                <Button Text="Send Request" 
                                        Clicked="Button_Clicked"
                            CommandParameter="{Binding Text,Source={x:Reference FriendUsernameEntry}}" />
                            </VerticalStackLayout>
                        </Border>

                        <!-- List of Incoming Requests -->
                        <Label Text="Incoming Requests" FontSize="Title" Padding="10,0"/>
                        <CollectionView ItemsSource="{Binding DimmerLiveViewModel.OnlineUsers}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:UserModelOnline">
                                    <Border Padding="10" Margin="5">
                                        <Grid ColumnDefinitions="*, Auto, Auto">
                                            <!-- Assumes Sender is included in the query -->
                                            <Label Grid.Column="0" Text="{Binding Username}" VerticalOptions="Center"/>
                                            <Button Grid.Column="1" Text="Accept" 
x:Name="AcceptBtn"
                                                    Clicked="AcceptBtn_Clicked"
                                                    CommandParameter="{Binding .}" />
                                            <Button Grid.Column="2" Text="Reject" x:Name="RejectBtn"
                                                    Clicked="RejectBtn_Clicked"
                                        CommandParameter="{Binding .}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </syncf:SfTabItem.Content>
            </syncf:SfTabItem>
         
        </syncf:SfTabView>



        </Grid>
    </ContentPage>