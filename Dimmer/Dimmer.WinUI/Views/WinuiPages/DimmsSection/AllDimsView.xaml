<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Dimmer.WinUI.Views.WinuiPages.DimmsSection.AllDimsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Dimmer.WinUI.Views.WinuiPages.DimmsSection"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>

            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />


        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" />

        <StackPanel Grid.Row="1">
            <ItemsRepeater>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="40" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <Image x:Name="SongImg" />
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock x:Name="Title" FontSize="22" />
                                <TextBlock x:Name="artistText" FontSize="19" />
                                <TextBlock x:Name="albumText" FontSize="15" />
                            </StackPanel>

                            <Button x:Name="MoreBtn" Click="MoreBtn_Click">
                                <SymbolIcon Symbol="More" />
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>

            </ItemsRepeater>

        </StackPanel>

    </Grid>
</Page>
