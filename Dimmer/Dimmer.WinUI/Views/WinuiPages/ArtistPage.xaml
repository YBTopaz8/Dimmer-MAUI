<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Dimmer.WinUI.Views.WinuiPages.ArtistPage"
    x:Name="MyArtistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Dimmer.Utilities.TypeConverters;assembly=Dimmer"
    xmlns:convertersWinUI="using:Dimmer.WinUI.Utils.Converters"
    xmlns:cw="using:Dimmer.WinUI.Views.WinuiPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:devWin="using:DevWinUI"
    xmlns:dgView="using:WinUI.TableView"
    xmlns:dimmSearch="clr-namespace:Dimmer.Data.ModelView.DimmerSearch;assembly=Dimmer"
    xmlns:lang="clr-namespace:Dimmer.Resources.Localization;assembly=Dimmer"
    xmlns:local="using:Dimmer.WinUI.Views.WinuiPages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:Microsoft.UI.Xaml.Media"
    xmlns:models="clr-namespace:Dimmer.Data.ModelView;assembly=Dimmer"
    xmlns:modelsDb="clr-namespace:Dimmer.Data.Models;assembly=Dimmer"
    xmlns:modeltwo="clr-namespace:Dimmer.WinUI.Utils.Models"
    xmlns:tql="clr-namespace:Dimmer.DimmerSearch.TQL;assembly=Dimmer"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:vm="clr-namespace:Dimmer.WinUI.ViewModel"
    PointerPressed="MyArtistPage_PointerPressed"
    mc:Ignorable="d"
    >


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GridView Grid.Row="0" ItemsSource="{x:Bind MyViewModel.SelectedArtist.AlbumsByArtist, Mode=OneWay}">
            <GridView.Header>
                <devWin:ShyHeader Title="{x:Bind MyViewModel.SelectedArtist.Name, Mode=OneWay}" Description="{x:Bind MyViewModel.SelectedArtist.Bio, Mode=OneWay}">
                    <devWin:ShyHeader.Profile>
                        <PersonPicture Width="150" Height="150" />
                    </devWin:ShyHeader.Profile>
                    <devWin:ShyHeader.Footer>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Footer Button" />
                            <Button Content="Footer Button" />
                            <Button Content="Footer Button" />
                        </StackPanel>
                    </devWin:ShyHeader.Footer>
                </devWin:ShyHeader>
            </GridView.Header>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>

                        <Image
                            Width="80"
                            Height="80"
                            Margin="4"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Source="{Binding ImagePath}"
                            Stretch="Fill"
                            />
                        <devWin:AutoScrollView>
                            <TextBlock Text="{Binding Name}" />
                        </devWin:AutoScrollView>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <dgView:TableView
            Grid.Row="1"
            AutoGenerateColumns="False"
            ItemsSource="{x:Bind MyViewModel.SelectedArtist.SongsByArtist}"
            >
            <dgView:TableView.Columns>
                <dgView:TableViewTemplateColumn>
                    <dgView:TableViewTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Width="80" Height="80">
                                <Image.Source>
                                    <BitmapImage UriSource="{Binding CoverImagePath}" />
                                </Image.Source>
                            </Image>
                        </DataTemplate>
                    </dgView:TableViewTemplateColumn.CellTemplate>
                </dgView:TableViewTemplateColumn>

                <dgView:TableViewTextColumn
                    Width="*"
                    Binding="{Binding Title}"
                    Header="Title"
                    />
                <dgView:TableViewTextColumn
                    Width="*"
                    Binding="{Binding OtherArtistsName}"
                    Header="Other Artists"
                    />
                <dgView:TableViewTextColumn
                    Width="*"
                    Binding="{Binding AlbumName}"
                    Header="Album"
                    />
                <dgView:TableViewTextColumn
                    Width="*"
                    Binding="{Binding GenreName}"
                    Header="Genre"
                    />
            </dgView:TableView.Columns>
        </dgView:TableView>

    </Grid>

</Page>
