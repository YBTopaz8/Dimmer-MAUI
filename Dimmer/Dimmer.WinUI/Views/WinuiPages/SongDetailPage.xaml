<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Dimmer.WinUI.Views.WinuiPages.SongDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:CustomViews="using:Dimmer.WinUI.Views.CustomViews.WinuiViews"
    xmlns:converters="using:Dimmer.WinUI.Utils.Converters"
    xmlns:cw="using:Dimmer.WinUI.Views.WinuiPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dgView="using:WinUI.TableView"
    xmlns:dimmSearch="clr-namespace:Dimmer.Data.ModelView.DimmerSearch;assembly=Dimmer"
    xmlns:lang="clr-namespace:Dimmer.Resources.Localization;assembly=Dimmer"
    xmlns:local="using:Dimmer.WinUI.Views.WinuiPages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Dimmer.Data.ModelView;assembly=Dimmer"
    xmlns:modelsDb="clr-namespace:Dimmer.Data.Models;assembly=Dimmer"
    xmlns:modeltwo="clr-namespace:Dimmer.WinUI.Utils.Models"
    xmlns:queryBuilder="queries"
    xmlns:tql="clr-namespace:Dimmer.DimmerSearch.TQL;assembly=Dimmer"
    xmlns:vm="clr-namespace:Dimmer.WinUI.ViewModel"
    x:Name="MyPage"
    Loaded="MyPage_Loaded"
    mc:Ignorable="d">
    <Page.Resources>

        <converters:CollectionSizeToVisibility x:Key="CollectionSizeToVisibility" />
        <converters:CollectionSizeToVisibilityInverted x:Key="CollectionSizeToVisibilityInverted" />
        <converters:EventDateToTimeAgo x:Key="EventDateToTimeAgo" />
        <converters:PlayTypeToBorderBrush x:Key="PlayTypeToBorderBrush" />
        <converters:StringNullOrEmptyToVisibility x:Key="StringNullOrEmptyToVisibility" />
        <converters:VolumeToPercentConverter x:Key="VolumeToPercentConverter" />
        <converters:BoolToImageConverter
            x:Key="PlayPauseConverter"
            FalseValue="play.png"
            TrueValue="pause.png" />
        <converters:BoolToImageConverter
            x:Key="FavIconConverter"
            FalseValue="heartbroken.png"
            TrueValue="heartlock.png" />
    </Page.Resources>


    <Grid PointerPressed="Grid_PointerPressed">
        <Image
            x:Name="BgImage"
            Opacity="0.05"
            Source="{x:Bind DetailedSong.CoverImagePath}"
            Stretch="Uniform" />
        <Grid x:Name="FgGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border
                Grid.Row="0"
                Width="150"
                Height="50"
                Margin="10,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="#111"
                CornerRadius="5"
                Visibility="Collapsed">
                <StackPanel
                    Margin="12"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Visibility="Collapsed">
                    <TextBlock
                        x:Name="CurrentSectionLabel"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Text="Overview" />
                </StackPanel>
            </Border>
            <!--  Main content area  -->
            <ScrollViewer
                x:Name="Scroller"
                Grid.Row="1"
                Margin="0,0,0,0"
                Background="Transparent"
                VerticalScrollBarVisibility="Auto"
                ViewChanged="ScrollViewer_ViewChanged">
                <StackPanel
                    x:Name="SegmentStack"
                    Padding="12"
                    Orientation="Vertical"
                    Spacing="20">

                    <!--  The elements involved in the animation  -->

                    <Grid
                        x:Name="SectionOverview"
                        PointerEntered="SectionOverview_PointerEntered"
                        PointerExited="SectionOverview_PointerExited"
                        ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />

                        </Grid.ColumnDefinitions>

                        <!--  The Image that will be the target of the connected animation  -->

                        <StackPanel Grid.Column="0" Spacing="10">
                            <Border
                                Width="300"
                                Height="300"
                                CornerRadius="8">
                                <Image x:Name="detailedImage" Source="{x:Bind DetailedSong.CoverImagePath}" />

                            </Border>
                            <StackPanel
                                x:Name="UtilitiesSection"
                                Orientation="Horizontal"
                                Spacing="10">
                                <Button Content="Edit" FontSize="19" />

                            </StackPanel>
                        </StackPanel>
                        <Border Grid.Column="1">

                            <StackPanel
                                x:Name="coordinatedPanel"
                                HorizontalAlignment="Left"
                                Spacing="5">

                                <TextBlock
                                    HorizontalAlignment="Left"
                                    FontSize="80"
                                    Style="{ThemeResource TitleTextBlockStyle}"
                                    Text="{x:Bind DetailedSong.Title, Mode=OneWay}"
                                    TextWrapping="Wrap" />

                                <StackPanel Orientation="Horizontal">
                                    <Button
                                        x:Name="ArtistBtn"
                                        HorizontalAlignment="Left"
                                        Background="Transparent"
                                        BorderThickness="2"
                                        Click="ArtistBtn_Click"
                                        Content="{x:Bind DetailedSong.ArtistName, Mode=OneWay}"
                                        FontWeight="Bold"
                                        Visibility="{x:Bind DetailedSong.ArtistToSong.Count, Converter={StaticResource CollectionSizeToVisibility}}" />
                                    <AutoSuggestBox
                                        x:Name="ArtistPickerAutoSuggestBox"
                                        Loaded="ArtistPickerAutoSuggestBox_Loaded"
                                        QuerySubmitted="ArtistPickerAutoSuggestBox_QuerySubmitted"
                                        SuggestionChosen="ArtistPickerAutoSuggestBox_SuggestionChosen"
                                        TextChanged="ArtistPickerAutoSuggestBox_TextChanged"
                                        Visibility="{x:Bind DetailedSong.ArtistToSong.Count, Converter={StaticResource CollectionSizeToVisibilityInverted}}" />

                                </StackPanel>
                                <StackPanel
                                    Margin="0,10,0,0"
                                    Orientation="Horizontal"
                                    Spacing="10">

                                    <Button
                                        HorizontalAlignment="Left"
                                        Background="Transparent"
                                        BorderThickness="2"
                                        Content="{x:Bind DetailedSong.AlbumName, Mode=OneWay}" />
                                    <TextBlock VerticalAlignment="Center" Text="{x:Bind DetailedSong.ReleaseYear, Mode=OneWay}" />

                                </StackPanel>

                                <Button
                                    Background="Transparent"
                                    BorderThickness="2"
                                    Content="{x:Bind DetailedSong.GenreName, Mode=OneWay}" />
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock FontSize="19" Text="Dimms" />

                                    <Button Content="{x:Bind DetailedSong.PlayCompletedCount, Mode=OneWay}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock FontSize="19" Text="Is Fav:" />

                                    <ToggleButton
                                        Height="20"
                                        VerticalAlignment="Bottom"
                                        Command="{x:Bind MyViewModel.AddFavoriteRatingToSongCommand}"
                                        CommandParameter="{Binding SelectedSong}"
                                        IsChecked="{x:Bind DetailedSong.IsFavorite, Mode=OneWay}" />
                                </StackPanel>

                                <RichTextBlock
                                    x:Name="BioBlock"
                                    Loaded="BioBlock_Loaded"
                                    TextWrapping="Wrap" />
                            </StackPanel>

                        </Border>
                    </Grid>

                    <StackPanel x:Name="SectionPlayback" Background="Transparent">

                        <!--<ItemsRepeater>
                            <ItemsRepeater.Layout>
                                <UniformGridLayout MaximumRowsOrColumns="3" />
                            </ItemsRepeater.Layout>

                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White"
                                        BorderBrush="Black"
                                        BorderThickness="1"
                                        Margin="6"
                                        Padding="12">

                                        <StackPanel>

                                        <TextBlock Text="Item" Foreground="Black"/>

                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>-->

                    </StackPanel>

                    <!--  SECTION 3: LYRICS  -->
                    <StackPanel x:Name="SectionLyrics" Background="Transparent">
                        <TextBlock
                            FontSize="34"
                            FontWeight="ExtraBold"
                            Text="Lyrics Section" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsRepeater ItemsSource="{x:Bind MyViewModel.SelectedSongLyricsObsCol}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate>
                                            <Border>
                                                <StackPanel
                                                    Background="Transparent"
                                                    Orientation="Horizontal"
                                                    Spacing="15">

                                                    <TextBlock FontSize="26" Text="{Binding Text}" />

                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>


                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </ScrollViewer>

                            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                <TextBlock
                                    FontSize="28"
                                    FontWeight="SemiBold"
                                    Text="Artists In This Song" />
                                <ItemsRepeater ItemsSource="{x:Bind MyViewModel.SelectedSong.ArtistToSong}">

                                    <ItemsRepeater.ItemTemplate>

                                        <DataTemplate>
                                            <Button
                                                Margin="0,10,0,0"
                                                BorderThickness="1"
                                                CornerRadius="15">
                                                <StackPanel
                                                    Background="Transparent"
                                                    Orientation="Horizontal"
                                                    Spacing="15">
                                                    <Image
                                                        Width="50"
                                                        Height="50"
                                                        Source="{Binding ImagePath}"
                                                        Visibility="{Binding ImagePath, Converter={StaticResource StringNullOrEmptyToVisibility}}" />
                                                    <TextBlock FontSize="26" Text="{Binding Name}" />

                                                </StackPanel>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>

                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <!--  SECTION 4: ANALYTICS  -->
                    <StackPanel x:Name="SectionAnalytics" Visibility="Collapsed">
                        <TextBlock
                            FontSize="34"
                            FontWeight="ExtraBold"
                            Text="Analytics Section" />


                        <Button x:Name="ManualFavoriteCount" Content="{Binding SelectedSong.ManualFavoriteCount}" />
                        <Button x:Name="PlayCompletedCount" Content="{Binding SelectedSong.PlayCompletedCount}" />
                        <Button x:Name="PlayCount" Content="{Binding SelectedSong.PlayCount}" />
                        <Button x:Name="PlayEventsCount" Content="{Binding SelectedSong.PlayEvents.Count}" />
                        <Button x:Name="ListenThroughRate" Content="{Binding SelectedSong.ListenThroughRate}" />
                        <Button x:Name="PlayStreakDays" Content="{Binding SelectedSong.PlayStreakDays}" />
                        <Button x:Name="PreviousCount" Content="{Binding SelectedSong.PreviousCount}" />
                        <Button x:Name="RankInAlbum" Content="{Binding SelectedSong.RankInAlbum}" />
                        <Button x:Name="RankInArtist" Content="{Binding SelectedSong.RankInArtist}" />
                        <Button x:Name="RepeatCount" Content="{Binding SelectedSong.RepeatCount}" />
                        <Button x:Name="RestartCount" Content="{Binding SelectedSong.RestartCount}" />
                        <Button x:Name="ResumeCount" Content="{Binding SelectedSong.ResumeCount}" />
                        <Button x:Name="SeekCount" Content="{Binding SelectedSong.SeekCount}" />
                        <Button x:Name="SkipCount" Content="{Binding SelectedSong.SkipCount}" />
                        <Button x:Name="SkipRate" Content="{Binding SelectedSong.SkipRate}" />
                        <Button x:Name="Rating" Content="{Binding SelectedSong.Rating}" />
                    </StackPanel>

                    <!--  SECTION 5: HISTORY  -->
                    <Grid
                        x:Name="SectionHistory"
                        Padding="10"
                        ColumnSpacing="5"
                        RowSpacing="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel
                            Grid.Row="0"
                            Grid.Column="0"
                            BorderBrush="DeepSkyBlue"
                            CornerRadius="10">
                            <Button
                                HorizontalAlignment="Stretch"
                                Background="Transparent"
                                BorderThickness="1">
                                <StackPanel>

                                    <StackPanel>
                                        <TextBlock
                                            HorizontalAlignment="Left"
                                            FontSize="20"
                                            FontWeight="SemiBold"
                                            Text="Dimms Completed" />
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            FontSize="27"
                                            FontWeight="SemiBold"
                                            Text="{Binding SelectedSong.PlayCompletedCount}" />
                                    </StackPanel>

                                    <StackPanel>
                                        <TextBlock
                                            HorizontalAlignment="Left"
                                            FontSize="20"
                                            FontWeight="SemiBold"
                                            Text="Eddington Number" />
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            FontSize="24"
                                            FontWeight="SemiBold"
                                            Text="{Binding SelectedSong.EddingtonNumber}" />
                                    </StackPanel>

                                </StackPanel>
                            </Button>


                        </StackPanel>

                        <StackPanel
                            Grid.Row="0"
                            Grid.Column="1"
                            BorderBrush="DeepSkyBlue"
                            CornerRadius="10">
                            <Button
                                HorizontalAlignment="Stretch"
                                Background="Transparent"
                                BorderThickness="1">
                                <StackPanel>
                                    <TextBlock
                                        HorizontalAlignment="Left"
                                        FontSize="20"
                                        FontWeight="SemiBold"
                                        Text="Play Streak Days" />
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        FontSize="24"
                                        FontWeight="SemiBold"
                                        Text="{Binding SelectedSong.PlayStreakDays}" />
                                </StackPanel>
                            </Button>


                        </StackPanel>

                        <StackPanel
                            Grid.Row="1"
                            Grid.Column="0"
                            BorderBrush="DeepSkyBlue"
                            CornerRadius="10">
                            <Button
                                HorizontalAlignment="Stretch"
                                Background="Transparent"
                                BorderThickness="1">
                                <StackPanel>
                                    <TextBlock
                                        FontSize="24"
                                        FontWeight="SemiBold"
                                        Text="Rank In Artist" />
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        FontSize="24"
                                        FontWeight="SemiBold"
                                        Text="{Binding SelectedSong.RankInArtist}" />
                                </StackPanel>
                            </Button>


                        </StackPanel>

                        <StackPanel
                            Grid.Row="1"
                            Grid.Column="1"
                            BorderBrush="DeepSkyBlue"
                            CornerRadius="10">
                            <Button
                                HorizontalAlignment="Stretch"
                                Background="Transparent"
                                BorderThickness="1">
                                <StackPanel>
                                    <TextBlock
                                        FontSize="24"
                                        FontWeight="SemiBold"
                                        Text="Rank In Album" />
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        FontSize="24"
                                        FontWeight="SemiBold"
                                        Text="{Binding SelectedSong.RankInAlbum}" />
                                </StackPanel>
                            </Button>


                        </StackPanel>

                        <!--<ScrollViewer >


                            <ItemsRepeater
                                Height="200"
                                ItemsSource="{x:Bind DetailedSong.PlayEvents, Mode=OneWay}"
                                Visibility="{x:Bind DetailedSong.PlayEvents.Count, Converter={StaticResource CollectionSizeToVisibility}}">
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel
                                            Padding="5"
                                            BorderBrush="{Binding PlayType, Converter={StaticResource PlayTypeToBorderBrush}}"
                                            BorderThickness="2"
                                            CornerRadius="20">
                                            <TextBlock Text="{Binding PlayTypeStr}" />
                                            <TextBlock Text="{Binding EventDate, Converter={StaticResource EventDateToTimeAgo}}" />
                                        </StackPanel>
                                    </DataTemplate>


                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>

                        </ScrollViewer>-->
                    </Grid>

                    <!--  SECTION 6: RELATED  -->
                    <StackPanel
                        x:Name="SectionRelated"
                        Height="400"
                        Spacing="10">
                        <TextBlock
                            FontSize="34"
                            FontWeight="ExtraBold"
                            Text="Related Section" />
                        <StackPanel
                            Padding="8"
                            HorizontalAlignment="Left"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            CornerRadius="10"
                            Orientation="Horizontal"
                            Spacing="10">

                            <TextBlock FontSize="24" Text="{Binding SelectedSongLastFMData.Statistics.Listeners}" />
                            <TextBlock
                                FontSize="24"
                                Text="Listeners"
                                ToolTipService.ToolTip="Total LastFM Listeners" />
                        </StackPanel>
                        <StackPanel
                            Padding="8"
                            HorizontalAlignment="Left"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            CornerRadius="10"
                            Orientation="Horizontal"
                            Spacing="10">

                            <TextBlock FontSize="24" Text="{Binding SelectedSongLastFMData.Statistics.PlayCount}" />
                            <TextBlock
                                FontSize="24"
                                Text="Dimms"
                                ToolTipService.ToolTip="Total LastFM Scrobbles" />
                        </StackPanel>
                        <TextBlock Text="Last FM Stats..." />
                        <TextBlock Text="{Binding SimilarTracks.Count}" />

                        <ScrollViewer Visibility="{Binding SimilarTracks.Count, Converter={StaticResource CollectionSizeToVisibility}}">

                            <ItemsRepeater ItemsSource="{Binding SimilarTracks}">
                                <ItemsRepeater.Layout>
                                    <UniformGridLayout
                                        ItemsJustification="Center"
                                        ItemsStretch="Uniform"
                                        MinColumnSpacing="15"
                                        Orientation="Horizontal" />
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate>
                                        <Border
                                            Margin="0,0,0,10"
                                            Padding="8"
                                            BorderBrush="Gray"
                                            BorderThickness="1"
                                            CornerRadius="10">
                                            <StackPanel
                                                x:Name="SimilarSongStackPanel"
                                                Width="140"
                                                PointerEntered="SimilarSongStackPanel_PointerEntered"
                                                PointerExited="SimilarSongStackPanel_PointerExited"
                                                Spacing="10">

                                                <TextBlock
                                                    FontSize="20"
                                                    FontWeight="SemiBold"
                                                    Text="{Binding Name}" />
                                                <TextBlock FontSize="16" Text="{Binding Artist.Name}" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>

                        </ScrollViewer>

                    </StackPanel>

                </StackPanel>
            </ScrollViewer>

        </Grid>
    </Grid>
</Page>