<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Dimmer.WinUI.Views.WinuiPages.Artist.AllArtistsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:localArtist="using:Dimmer.WinUI.Views.WinuiPages.Artist"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converters="clr-namespace:Dimmer.Utilities.TypeConverters;assembly=Dimmer"
    xmlns:convertersWinUI="using:Dimmer.WinUI.Utils.Converters"
    
    xmlns:dgView="using:WinUI.TableView"
    xmlns:dimmSearch="clr-namespace:Dimmer.Data.ModelView.DimmerSearch;assembly=Dimmer"
    xmlns:lang="clr-namespace:Dimmer.Resources.Localization;assembly=Dimmer"
    xmlns:local="using:Dimmer.WinUI.Views.CustomViews.WinuiViews"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:Microsoft.UI.Xaml.Media"
    xmlns:models="clr-namespace:Dimmer.Data.ModelView;assembly=Dimmer"
    xmlns:modelsDb="clr-namespace:Dimmer.Data.Models;assembly=Dimmer"
    xmlns:modeltwo="clr-namespace:Dimmer.WinUI.Utils.Models"
    xmlns:queryBuilder="queries"
    xmlns:tql="clr-namespace:Dimmer.DimmerSearch.TQL;assembly=Dimmer"
    xmlns:vm="clr-namespace:Dimmer.WinUI.ViewModel"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>

            <DataTemplate x:Key="LinedFlowLayoutItemTemplate">
                <ItemContainer AutomationProperties.Name="{Binding Name}">
                    <Grid>
                        <Image Stretch="UniformToFill" HorizontalAlignment="Center" 
                               Height="110" Opacity="0.7"
                               VerticalAlignment="Center" Width="130">
                            <Image.Source>
                                <BitmapImage UriSource="{Binding ImagePath}"/>
                            </Image.Source>
                             
                        </Image>
                        <Grid>
                            <Border VerticalAlignment="Bottom" Height="40"  Background="DarkSlateBlue" Opacity=".75">
                                
                            </Border>
                            <StackPanel Orientation="Vertical" Height="40" Padding="5,1,5,1" VerticalAlignment="Bottom">
                            <TextBlock Text="{Binding Name}" Foreground="White"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding TotalCompletedPlays}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="White"/>
                                <TextBlock Text=" Plays" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="White"/>
                            </StackPanel>
                        </StackPanel>

                        </Grid>
                    </Grid>
                </ItemContainer>
            </DataTemplate>
        </ResourceDictionary>
    </Page.Resources>
    <ScrollView>
        <StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                <TextBlock Text="{x:Bind MyViewModel.ArtistsCollection.Count}"/>
                <TextBlock Text="Artists"/>
            </StackPanel>


            
                <GridView HorizontalAlignment="Left" ItemsSource="{x:Bind MyViewModel.ArtistsCollection, Mode=OneWay}"
                     ItemTemplate="{StaticResource LinedFlowLayoutItemTemplate}" 
    x:Name="StyledGrid"
                          >

                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem" BasedOn="{StaticResource DefaultGridViewItemStyle}">
                            <Setter Property="Margin" Value="5, 5, 5, 5"/>
                        </Style>
                    </GridView.ItemContainerStyle>

                    <!-- An extra property also needs to be added to the GridView's ItemsWrapGrid.
    The following sets the maximum # of items to show before wrapping.-->
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid x:Name="MaxItemsWrapGrid"
                           MaximumRowsOrColumns="15" 
                           Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                </GridView>

        </StackPanel>
    </ScrollView>
</Page>
