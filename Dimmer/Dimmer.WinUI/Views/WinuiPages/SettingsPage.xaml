<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Dimmer.WinUI.Views.WinuiPages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dimmer.WinUI.Views.WinuiPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">

            <!-- Wizard content -->
            <FlipView x:Name="WizardFlipView"
                      SelectionChanged="WizardFlipView_SelectionChanged"
                      
                      >
                <!--IsSwipeEnabled="True"-->
                <!-- Step 1 -->
                <Grid Background="#1C1C1C" Padding="40">
                    <StackPanel>
                        <TextBlock Text="Step 1: Account Info"
                                   FontSize="28"
                                   Foreground="White"
                                   Margin="0,0,0,10"/>
                        <TextBox PlaceholderText="Enter your name" Margin="0,0,0,10"/>
                        <TextBox PlaceholderText="Enter your email" Margin="0,0,0,10"/>
                    </StackPanel>
                </Grid>

                <!-- Step 2 -->
                <Grid Background="#202020" Padding="40">
                    <StackPanel>
                        <TextBlock Text="Step 2: Preferences"
                                   FontSize="28"
                                   Foreground="White"
                                   Margin="0,0,0,10"/>
                        <CheckBox Content="Enable notifications" Margin="0,0,0,10"/>
                        <CheckBox Content="Use dark mode" Margin="0,0,0,10"/>
                    </StackPanel>
                </Grid>

                <!-- Step 3 -->
                <Grid Background="#252525" Padding="40">
                    <StackPanel>
                        <TextBlock Text="Step 3: Confirmation"
                                   FontSize="28"
                                   Foreground="White"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="Review your info before finishing."
                                   Foreground="White"
                                   Margin="0,0,0,20"/>
                        <Button Content="Finish"
                                HorizontalAlignment="Right"
                                Click="Finish_Click"/>
                    </StackPanel>
                </Grid>
            </FlipView>

            <!-- Navigation buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="BackButton"
                        Content="◀ Back"
                        Click="BackButton_Click"
                        Margin="0,0,10,0"/>
                <Button x:Name="NextButton"
                        Content="Next ▶"
                        Click="NextButton_Click"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Page>
