<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Dimmer.WinUI.Views.WinuiPages.Settings.BackupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Dimmer.Utilities.TypeConverters;assembly=Dimmer"
    xmlns:convertersWinUI="using:Dimmer.WinUI.Utils.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dgView="using:WinUI.TableView"
    xmlns:local="using:Dimmer.WinUI.Views.WinuiPages.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Dimmer.Data.ModelView;assembly=Dimmer"
    xmlns:modelsDb="clr-namespace:Dimmer.Data.Models;assembly=Dimmer"
    xmlns:modeltwo="clr-namespace:Dimmer.WinUI.Utils.Models"
    Padding="10"
    mc:Ignorable="d"
    >

    <ScrollViewer Background="#1e1e1e" VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="10">
                    <TextBlock Text="Total Number of Songs" />
                    <TextBlock Text="Total Number of Artists" />
                    <TextBlock Text="Total Number of Albums" />
                    <TextBlock Text="Total Number of Genres" />
                    <TextBlock Text="Total Number of Playlists" />
                    <TextBlock Text="Total Number of Favorite Songs" />
                    <TextBlock Text="Total Number of Dimmer Events" />
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="10">
                    <TextBlock Text="{x:Bind MyViewModel.TotalNumberOfSongs}" />
                    <TextBlock Text="{x:Bind MyViewModel.TotalNumberOfArtists}" />
                    <TextBlock Text="{x:Bind MyViewModel.TotalNumberOfAlbums}" />
                    <TextBlock Text="{x:Bind MyViewModel.TotalNumberOfGenres}" />
                    <TextBlock Text="{x:Bind MyViewModel.TotalNumberOfPlaylists}" />
                    <TextBlock Text="{x:Bind MyViewModel.TotalNumberOfFavoriteSongs}" />
                    <TextBlock Text="{x:Bind MyViewModel.TotalNumberOfDimmerEvents}" />
                </StackPanel>
            </Grid>
            <StackPanel Grid.Row="1" >
                <Button
                    x:Name="BackUpDataBtn"
                    Click="BackUpDataBtn_Click"
                    Content="Backup AppData"
                    />

                <InfoBar
                    x:Name="StatusInfoBar"
                    Title="Success!"
                    Content="Backup Successful!"
                    Tapped="StatusInfoBar_Tapped"
                    Visibility="Collapsed"
                    />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
