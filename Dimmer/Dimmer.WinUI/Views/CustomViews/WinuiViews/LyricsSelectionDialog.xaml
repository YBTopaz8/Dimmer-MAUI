<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Dimmer.WinUI.Views.CustomViews.WinuiViews.LyricsSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Select Lyrics for Story"
    Width="600"
    Height="500"
    DefaultButton="Primary"
    PrimaryButtonText="Share"
    SecondaryButtonText="Cancel"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,*">
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            HorizontalAlignment="Center"
            FontSize="14"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}">
            <Run x:Name="SelectionCountText" Text="0 / 5 lines selected" />
        </TextBlock>

        <ScrollViewer Grid.Row="1">
            <ItemsRepeater x:Name="LyricsRepeater" Margin="0,8,0,0">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <CheckBox
                            Margin="0,4"
                            Content="{x:Bind}"
                            IsEnabled="True" />
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</ContentDialog>
