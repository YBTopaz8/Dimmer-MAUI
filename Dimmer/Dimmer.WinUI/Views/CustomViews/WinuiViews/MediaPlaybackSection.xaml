<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Dimmer.WinUI.Views.CustomViews.WinuiViews.MediaPlaybackSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dimmer.WinUI.Views.CustomViews.WinuiViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converters="using:Dimmer.WinUI.Utils.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" x:Name="TopPanel" Background="Transparent"
              PointerReleased="TopPanel_PointerReleased">
            <StackPanel Orientation="Horizontal" Spacing="10" Margin="10" HorizontalAlignment="Center">
                
                    <Image Source="/Dimmer/Resources/Images/skipprevious.png" 
                Height="20" Width="20"/>
                
                <Image Source="/Dimmer/Resources/Images/play.svg" Height="20" Width="20"/>
                
                <Image Source="/Dimmer/Resources/Images/skipnext.svg" Height="20" Width="20"/>
            <Slider Value="{x:Bind MyViewModel.CurrentTrackPositionPercentage}"
Minimum="0" Maximum="100" VerticalAlignment="Center"
   Width="400"/>
                <TextBlock 
                    VerticalAlignment="Center"
                    Text="{x:Bind MyViewModel.CurrentTrackPositionSeconds, Mode=OneWay, Converter={StaticResource DurationConverter}}"
     />
                <TextBlock 
                    VerticalAlignment="Center"
                    Text="{x:Bind MyViewModel.CurrentTrackDurationSeconds, Mode=OneWay, Converter={StaticResource DurationConverter}}"
     />
            </StackPanel>



        </StackPanel>
    </Grid>
</UserControl>
