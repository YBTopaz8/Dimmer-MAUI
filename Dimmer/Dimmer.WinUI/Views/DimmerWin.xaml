<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Dimmer.WinUI.Views.DimmerWin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:converters="using:Dimmer.WinUI.Utils.Converters"
    xmlns:customViews="using:Dimmer.WinUI.Views.CustomViews.WinuiViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Dimmer.WinUI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="DimmerWin"
    Activated="Window_Activated"
    xmlns:localViews="using:Dimmer.WinUI.Views.CustomViews.WinuiViews"
    Closed="DimmerWindowClosed"
    SizeChanged="Window_SizeChanged"
    
    mc:Ignorable="d">


    <NavigationView x:Name="nvSample"
    IsSettingsVisible="False"
    IsBackButtonVisible="Visible"
    IsBackEnabled="True" Tapped="nvSample_Tapped"
    BackRequested="nvSample_BackRequested"                    SelectionChanged="nvSample_SelectionChanged"
    ItemInvoked="nvSample_ItemInvoked" 
    AlwaysShowHeader="True"
    PaneTitle="Pane Title"
    PaneDisplayMode="Left"
    ExpandedModeThresholdWidth="500"
    SelectionFollowsFocus="Disabled"
    IsTabStop="False">
        <NavigationView.Header>
            <customViews:MediaPlaybackSection
Loaded="TopMediaControlSection_Loaded"
 x:Name="TopMediaControlSection" Grid.Row="1" />


        </NavigationView.Header>

        <NavigationView.MenuItems>

            <NavigationViewItem Content="All Songs" Tag="AllSongsItem" x:Name="AllSongsItem">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xEC4F;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItem Content="View Queue" Tag="ViewQueueItem" x:Name="ViewQueueItem">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xEA41;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            
            <NavigationViewItemHeader Content="Actions"/>

            <NavigationViewItem 
                                Tag="SamplePage2" x:Name="ArtistsItem" SelectsOnInvoked="True">
                <NavigationViewItem.Content>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock 
                                Loaded="ArtistsItem_Loaded" x:Name="ArtistsLabelView" Text="Artists"/>
                        
                        </StackPanel>
                </NavigationViewItem.Content>
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Contact" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

        </NavigationView.MenuItems>

        
        
        <NavigationView.AutoSuggestBox>
           
            <AutoSuggestBox
                            x:Name="SearchTextBox"
                            Width="435"
                            AccessKey="F" 
                            AccessKeyInvoked="SearchAutoSuggestBox_AccessKeyInvoked"
                            BorderThickness="2"
                            KeyboardAcceleratorPlacementMode="Auto"
                            PlaceholderText="Search with TQL..."
                            Text="{x:Bind MyViewModel.CurrentTqlQuery, Mode=OneWay}"
                            TextChanged="SearchAutoSuggestBox_TextChanged" />
        </NavigationView.AutoSuggestBox>

        <NavigationView.PaneFooter>
            <StackPanel x:Name="FooterStackPanel" Orientation="Vertical" Visibility="Visible">
                </StackPanel>
        </NavigationView.PaneFooter>
        



        <Grid x:Name="MainGrid" Background="Transparent"
          KeyDown="MainGrid_KeyDown">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5" Loaded="DimmerStatusPanel_Loaded"
                        x:Name="DimmerStatusPanel"
     Visibility="{x:Bind MyViewModel.DimmerProgressBarView.Visibility, Mode=OneWay}">


                    <ProgressBar IsIndeterminate="True"
                         
                         Value="30"
     Visibility="{x:Bind MyViewModel.DimmerProgressBarView.Visibility, Mode=OneWay}"
                         Margin="10,5" 
                         Loaded="DimmerProgressBar_Loaded"
                         x:Name="DimmerProgressBar"
                         />

                    <TextBlock  HorizontalAlignment="Center"
                       x:Name="DimmerStatusTextBlock"
     Visibility="{x:Bind MyViewModel.DimmerProgressBarView.Visibility, Mode=OneWay}"
     Text="{x:Bind MyViewModel.LatestAppLog.Message, Mode=OneWay}"
                       Loaded="DimmerStatusTextBlock_Loaded"/>



                </StackPanel>
                <Button Grid.Column="1"  Click="ClearStatus_Click" 
                    Background="Transparent"
                    x:Name="ClearStatus" HorizontalAlignment="Right">
                    <SymbolIcon Symbol="Clear" Height="15" Width="15" />
                </Button>
            </Grid>
           

            <Frame
            x:Name="ContentFrame"
            Grid.Row="2"
            x:FieldModifier="Public" />

            <localViews:SmokeViewQueueGrid x:Name="SmokeGrid" Visibility="Collapsed" Grid.RowSpan="3" DismissRequested="SmokeGrid_DismissRequested"
                            Loaded="SmokeGrid_Loaded"/>

        </Grid>


    </NavigationView>

</Window>
