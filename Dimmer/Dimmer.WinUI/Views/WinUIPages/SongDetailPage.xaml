<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Dimmer.WinUI.Views.WinUIPages.SongDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dimmer.WinUI.Views.WinUIPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="">

    

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- A simple header with a back button -->
        <StackPanel Grid.Column="0" Margin="12">
                <Button x:Name="BackButton" Content="&#xE72B;" FontFamily="Segoe MDL2 Assets" Click="BackButton_Click" />
            </StackPanel>

            <!-- Main content area -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="20" Padding="24" Orientation="Horizontal">

                    <!-- The elements involved in the animation -->
                    <Grid HorizontalAlignment="Center">
                        <!-- The Image that will be the target of the connected animation -->
                        <Image x:Name="detailedImage" 
                           Source="{x:Bind DetailedSong.CoverImagePath}"
                           Width="300" 
                           Height="300"/>
                    </Grid>

                    <!-- This panel's animation will be coordinated with the main one -->
                    <StackPanel x:Name="coordinatedPanel" Spacing="5" HorizontalAlignment="Center">
                        <TextBlock Text="{x:Bind DetailedSong.Title}" Style="{ThemeResource TitleTextBlockStyle}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                        <TextBlock Text="{x:Bind DetailedSong.ArtistName}" Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                        <TextBlock Text="{x:Bind DetailedSong.AlbumName}" Style="{ThemeResource BodyStrongTextBlockStyle}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- More info can go here -->
                    <StackPanel Margin="0,20,0,0">
                        <TextBlock Text="More Details..." Style="{ThemeResource SubheaderTextBlockStyle}" />
                        <!-- Add more details like Genre, Year, etc. -->
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Grid>
</Page>